<div class="row">
    {{#each definitions}}
        <div class="col-md-6">
            <h3><a name="/definitions/{{@key}}"></a>{{@key}}</h3>
            {{json .}}
        </div>
    {{/each}}
</div>
