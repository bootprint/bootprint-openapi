{{! renders a json-schema model within swagger (calls json-schema-partials).}}

{{! "title" is usually past as parameter, call the json-schema partials if necessary}}
{{#if .}}
    {{>json-schema/main-panel title=title}}
{{/if}}